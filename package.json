{
  "name": "backend-boilerplate",
  "version": "0.0.1",
  "scripts": {
    "build": "dotnet build ./backend/backend.sln",
    "start": "cd ./backend/backend && dotnet run",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^5.4.0"
  }
}
